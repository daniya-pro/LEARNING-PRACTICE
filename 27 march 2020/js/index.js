var giiveano = parseInt(prompt('1stno', 1))
var secno = parseInt(prompt('2nd no', 1))
var a = alert(giiveano + secno)
















//sec work
var promp1 = parseInt(prompt("write no "))
var type = ""
for (var i = 0; i < 13; i++) {
    var result = promp1 + ' x ' + i + " = " + promp1 * i;
    type = type + result + '\n'

}
alert(type)